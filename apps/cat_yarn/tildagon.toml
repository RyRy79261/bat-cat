[app]
name = "bat-cat"
category = "Badge"

[publish]
# Mono-internal: target repo for the store-publish workflow (stripped from the
# published copy). This is the monorepo itself, so the workflow publishes via
# this repo's `store` branch using the built-in GITHUB_TOKEN — no PAT needed.
repo = "RyRy79261/bat-cat"

[metadata]
author = "RyRy79261"
license = "MIT"
url = "https://github.com/RyRy79261/bat-cat"
description = "Ambient toy: cats chase a yarn ball that rolls with tilt. The rim shows battery."
version = "1.00.00"
